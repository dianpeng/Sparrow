#include "ir-helper.h"

/* Dumpa the the IR-Graph into textual format */

static void dtext_visit( const struct IrGraph* graph , const struct IrNode* prev ,
                                                       const struct IrNode* now  ,
                                                       FILE* output ) {
  /* Write out the NOW node attributes */
}
